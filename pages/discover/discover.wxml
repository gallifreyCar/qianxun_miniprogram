<!--pages/discover/discover.wxml-->
<navigator class="discoverView"  wx:key="id" wx:for="{{discoverList}}" url="../../subDiscover/pages/{{item.name}}/{{item.name}}">
<image src="{{item.image}}"></image>
<text>{{item.title}}</text>
</navigator>
